# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# cocotb / simulation
sim_build/
tb/sim_build/
results.xml
tb/results.xml
*.vcd
*.fst
*.ghw
dump.*
obj_dir/
verilator.log

# synthesis
build/
*.json
# Measured results are committed, so a clone can redraw every figure and check every
# quoted number without the PDK, which no hosted runner has.
!docs/synth/*.json
!docs/pdk/*.json
!docs/pnr/*.json

# software
*.elf
*.o
*.a
*.bin
*.hex
*.dis
*.map

# editors / OS
*.swp
.DS_Store

# PnR working trees. LibreLane runs are large and reproducible; docs/pnr keeps the
# metrics and docs/img the rendered layout.
build/pnr/
pnr/runs/
